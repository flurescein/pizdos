// ---------------------------- Imports. ---------------------------------
const dos = require('../src/dos')

class Pizdos {
    static startAttack(url, options) {
        console.log(url)
    }
}